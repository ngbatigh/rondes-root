- choix option B.

- creer une variable globale de type tableau varSession["session", "id-operateur", "id=type-ronde", "id-compteur", "date-releve", "heure-releve"].

- creer un panneau de loging qui va stocker l'id-operateur dans varSession.
  - le panneau de loging porte les champs nom d'utilisateur et mot de passe
  - ajouter un lien "bypass->"en bas sur ce panneau de loging qui permet en mode test de contourner le loging
  - apres validation:
    - enregistrer l'id-operateur dans varSession.
    - afficher l'operateur dans le select ronde du formulaire.
    - rendre le select operateur inactif durant tout le reste de la session.
    - si la "fonction" de l'operateur est different de "admin", desactiver les panneaux suivantes:
      - ajouterOperateurPanel
      - modifierOperateurPanel
      - supprimerOperateurPanel
      - qrCodeCompteurPanel
      - activerCompteurPanel
      - modifierCompteurPanel
      - clonerCompteurPanel
      - creerCompteurPanel
      - ajouterRondePanel
      - supprimerRondePanel

- apres loging un panneau de "choix ronde" grace a des check boxe a choix unique permet de :
  - de choisir "Revue" ou "ronde". une attention particuliere doit etre porte sur la disposition de ces options pour un bon affichage.
  - si c'est l'option "ronde"qui est choisi :
    - un select type ronde aparait pour selectionner le type de ronde(obligatoire pour continuer dans ce cas)
    - enregistrer le type de ronde selectionne dans varSession.
    - afficher le type de ronde dans le select ronde.
    - rendre le select ronde inactif durant tout le reste de la session.
  - un bouton valider permet de passer au panneau affichant la forme actuelle de l'application.

- inserrer une image scanner qr code (design d'un gros bouton carre) a gauche du groupe label + select du form-groupe compteur.
- ce boutton va permettre de scanner les code qr. le code de scan doit s'inspirer exactement du fichier qr-scanner.html
  - le bouton scanner a nouveau doit etre renomme "continuer" et permet
    - de revenir au formulaire en actualisant varSession
    - d'afficher le compteur dans le select compteur
    - et rendre le select inactif.
  - le bouton annuler permet de
    - revenir au formulaire
    - activer le select compteur
    - charger la liste des compteurs dans select compteur pour une selection manuelle
- apres validation du formulaire, un panneau recap aparait avec la liste des donnes pour ce relevé a savoir:
  - nom operateur
  - prenom operateur
  - ronde
  - compteur
  - date/heure
  - valeur
- un bouton confirmation permet de confirmer et un bouton annuler permet d'annuler l'evenement submit
- si confirmation, une boite de dialogue avec deux boutons apparait:
  - bouton continuer la ronde; permet de revenir au formulaire de relevé
  - bouton finir la ronde permet d'afficher un panneau avec la liste de tous les compteurs relevé avec les valeurs et les dates classés par heure du plus ancien au plus récent. deux bouton:
    - fin permet de declencher un submit pour gerer l'envoie de toutes les données des relevés de la session vers le back end et fermer l'application sur confirmation
    - nouvelle session permet de declencher toujours un submit pour gerer l'envoie de toutes les données des relevés de la session vers le back end et redemarrer l'application.

- toute creation doit rester dans le meme theme generale concernant le style general
